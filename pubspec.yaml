name: platon_tv
description: تطبيق بث مباشر قناة beIN Sports 2
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  better_player: ^0.0.83

flutter:
  uses-material-design: true
